{
  "servers": {
    "figma": {
      "command": "npx -y @modelcontextprotocol/server-figma",
      "env": {
        "FIGMA_ACCESS_TOKEN": "${FIGMA_ACCESS_TOKEN}"
      },
      "description": "Figma MCP server. Set FIGMA_ACCESS_TOKEN in environment before starting."
    }
  }
}
